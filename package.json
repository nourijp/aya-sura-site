{
  "name": "aya-sura-site",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check-links": "node scripts/check-links.mjs",
    "postbuild": "node scripts/check-links.mjs"
  },
  "dependencies": {
    "astro": "^4.16.0"
  }
}
